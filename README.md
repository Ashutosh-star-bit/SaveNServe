🍽️ Save N Serve: Waste Less, Feed MoreSave N Serve is a full-stack web application designed to combat food waste by efficiently connecting food donors (such as mess staff and event organizers) with organizations (NGOs and animal shelters) that can redistribute surplus food to those in need.The system features a dual-role dashboard for managing donations and requests, real-time activity tracking, and a map interface to locate nearby organizations.✨ FeaturesUser Authentication: Secure Sign-up and Login system with distinct roles: Mess Staff (Donors) and NGO / Shelter (Requesters).Report Surplus Food: Mess Staff can easily report and submit available surplus food donations.Large Event Donations: Dedicated form for large-scale donations (e.g., weddings, conferences) to notify NGOs in advance.Food Requests: NGOs can submit requests for food pickup.Dashboard Management: Separate views for Staff (to manage incoming requests) and NGOs (to manage available donations). Allows accepting or declining items.Feed Animals: Route food unsuitable for human consumption directly to trusted animal shelters.Map Tracker: Uses the Google Maps JavaScript API to visualize the location of NGOs and shelters.Activity History & Stats: View recent activity and track key statistics like total meals saved and pending items.💻 Tech StackComponentTechnologyDescriptionBackendPython, FlaskRESTful API endpoints for application logic and database interaction.DatabaseMySQL, mysql.connectorUsed to store user data, donations, requests, and activity logs.FrontendHTML5, CSS3, JavaScriptResponsive user interface with custom styling.Frontend LogicVanilla JS, Fetch APIHandles form submission, user authentication flow, and API communication.⚙️ Setup and InstallationFollow these steps to get the application running on your local machine.1. Backend DependenciesInstall Python and the necessary libraries:Bash# 1. Install Flask and MySQL Connector
pip install Flask mysql-connector-python

# 2. Navigate to the project directory containing app.py
cd SaveNServe/
2. Database Configuration (MySQL)The application requires a MySQL database.Start your MySQL Server.Create the Database: The application is configured to use the database name savenserve_db.Update Credentials: Open SaveNServe/app.py and update the MYSQL_CONFIG dictionary with your local MySQL credentials, particularly the password for the root user.PythonMYSQL_CONFIG = {
    'user': 'root',
    'password': 'your_mysql_password', # <-- UPDATE THIS
    'host': 'localhost',
    'database': 'savenserve_db'
}
Note: The application's signup and other API routes will automatically attempt to create the necessary tables (users, donations, food_requests, event_donations, animal_feed, feedback) when first interacted with.3. Google Maps Integration (Optional)To enable the map functionality, you must include a valid Google Maps JavaScript API Key:Open SaveNServe/scripts/config.js.Replace the placeholder value with your actual Google Maps API Key.JavaScriptconst GOOGLE_MAPS_API_KEY = "AIzaSyB3HyOfewE7GsOmO-ioOAnzsqlmTVhftOg"; // Replace with your key
🚀 Running the ApplicationRun the Flask Backend:Bashpython app.py
The application will start on http://0.0.0.0:5000 (or http://127.0.0.1:5000).Access the Frontend:Open your web browser and navigate to:http://127.0.0.1:5000/index.htmlDemo CredentialsYou can test the system using the following demo accounts on the Login page:RoleUsernamePasswordMess Staff (Donor)staff1234NGO (Requester)ngo1234Student (General)student1234(Or create a new account via Sign Up)
